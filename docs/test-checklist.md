# 개발 검증 체크리스트

- [x] 사이드바 하단의 데이터 기준 카드와 접기·고정 버튼이 노출되지 않고, 상단 메뉴 버튼으로 접기·펼치기가 유지되는지 정적 검사한다.
- [x] 운행기록 상단에서 본부·부·팀 카드를 제거하고, 차량현황 등록 차량 요약의 작은 조직 수 배지로 옮긴 구조를 `driving-summary-layout.cjs`로 정적 검사한다. 실제 넓은 화면·작은 화면 배치는 배포 후 확인한다.
- [x] 차량현황·차량계약정보의 팀과 담당자(정) 사이 CC 열, 차량 업로드·내려받기 양식, 직접 입력, 차량번호 기반 계약 자동 연결과 `018_vehicle_cc.sql` 저장 구조를 `vehicle-cc.cjs`로 정적 검사한다. 2026-09-26 Supabase SQL Editor에서 `vehicles.cc` 열을 단독 생성한 뒤 `pg_notify('pgrst', 'reload schema')`로 API 스키마 캐시를 갱신했고, 배포 홈페이지와 같은 REST API의 `vehicles?select=cc` 요청이 HTTP 200으로 성공한 것을 확인했다. 실제 Excel 업로드·계약 화면 표시 확인은 사용자 재시도 후 진행한다.
- [x] CC 열 추가 뒤 계약 표의 필터 위치와 업로드 팝업의 필수 열 안내를 갱신했다. `vehicle-cc.cjs`로 CC 필터 위치·안내·저장 구조를 정적 검사한다. 첨부 Excel의 첫 행과 CC 입력값도 읽기 전용으로 확인했다.

## 2026-09-19 회원가입·회원관리

- 환경 설정 메뉴 제거, 로그인/회원가입 전환, 관리자 전용 회원관리 화면, 가입·관리 RPC 호출을 정적 검사한다.
- 신규 계정 disabled, 기존 계정 active 유지, 역할·상태 유효성, 현재 관리자 자기잠금 방지, 활성 사용자 RLS를 SQL 정적 검사한다.
- 일반회원은 차량현황·인수인계만 표시하고 차량 편집 UI를 숨기며, 계약·운행 서버 조회는 차단하고 인수인계 등록은 허용하는 규칙을 검사한다.
- 실제 SQL 실행 성공. 확인 쿼리에서 profiles 상태 열, 관리자 RPC, 활성 상태 함수, 업무 RLS 7개, 인수인계 Storage RLS 3개가 모두 `true`로 확인됐다.
- 이메일 확인, 가입 → 승인 → 역할별 로그인 허용/차단은 별도 실서버 검증이 필요하다.
- 입력자 역할 제거 SQL 실행 성공. 입력자 계정 0, admin/viewer 역할 제약조건, 관리자 RPC, 관리자 전용 운행 정책이 모두 `true`로 확인됐다.
- 2026-09-22 `012_member_status_simplification.sql` 실행 성공. 기존 `pending` 상태는 삭제 없이 `disabled`로 전환했고, `profiles_status_check`가 `active`·`disabled` 두 값만 허용하는 것을 실제 SQL 결과로 확인했다.
- 관리자 비밀번호 초기화 버튼·확인 절차, 본인 계정 차단, `013_admin_password_reset.sql`의 관리자 검증·초기 비밀번호 bcrypt 처리·실행 권한을 정적 검사한다. 2026-09-22 SQL 실행 성공으로 함수 생성과 실행 권한을 적용했으며, 실제 초기화한 직원 계정의 `1234` 로그인은 후속 실서버 검증이 필요하다.

## 2026-09-19 인수인계 입력 UI·사진 6장 제한

- handover.js 문법 검사 및 handover mock 검증 통과. 사진 6장 허용·7장 거부, 형식·개별/전체 크기 제한 확인.
- dashboard-layout과 closing-ui 회귀검증 통과. 실제 브라우저의 날짜·선택·텍스트영역 시각 정렬 및 사진 선택은 미검증.
- 후속 오류 수정: 실제 `#handoverForm` CSS 선택자와 select/textarea 스타일 규칙 존재를 mock 검사에 추가.
- 차량번호 직접 입력 후보·선택 후 기본정보 표시와 첫 줄/인계일·인계자·인수자 3열 배치를 정적 검사한다. 실제 브라우저에서 후보 선택·인계자 자동 입력과 좁은 화면 줄바꿈은 배포 후 확인한다.
- 차량 선택·입력 뒤에도 인계자 칸을 자동으로 채우지 않고 직접 입력값을 유지하는지 정적 검사한다.
- 사고접수·인수인계 차량 기본정보에 담당자(정)·담당자(부)가 모두 표시되는지 정적 검사한다.
- Supabase 표·비공개 Storage·RLS 및 화면의 업로드/저장/서명 URL 호출을 정적 검사한다.
- 실제 `005_handovers.sql` 실행 성공. 확인 쿼리에서 `vehicle_handovers` 표, 5MB 비공개 `handover-photos` 버킷, 5개 정책이 모두 `true`로 확인됐다.
- 외관 사진 업로드·재접속 조회, admin/editor 저장 허용과 viewer 저장 차단은 별도 실서버 검증이 필요하다.
- PDF 허용 MIME, 최대 6개·파일당 5MB·총 20MB 제한과 PDF 상세 링크를 코드·정적 테스트에 반영했다. 실제 버킷 MIME 갱신 후 `pdf_allowed=true`를 확인했으며 PDF 파일 업로드 자체는 추가 확인이 필요하다.
- 파일 선택을 여러 번 했을 때 기존 첨부와 새 첨부가 합쳐지고 동일 파일은 한 번만 유지되는 누적 로직을 mock 검증했다.
- 인수자 계정 선택, 차량번호 이력 검색, 지정 인수자만 동의 가능한 RPC와 동의 ID·시각 기록 SQL을 추가했다. 2026-09-21 `008_handover_recipient_consent.sql` 실행 성공 후 활성 회원 목록과 기존 이력의 `기존 이력` 표시를 실서버에서 확인했다. 실제 다른 계정의 동의 차단/지정 계정의 동의 완료는 별도 실서버 검증이 필요하다.
- 상세 열기 영역을 표 행에서 이동시키던 구조를 동적 상세 카드 생성 방식으로 변경해, 화면 이동 후 이력 새로고침이 일어나도 상세 버튼이 계속 동작하도록 정적 검사한다. 동의 완료 상세에는 이름·시각만 표시하고 Auth ID는 숨긴다.
- `handover-consent.cjs` 정적 검사로 인수자 선택·차량번호 검색·동의 RPC 호출과 수신자/동의 로그 SQL 규칙을 확인한다.
- 기존 이력의 수신자 이름과 활성 회원의 정확한 1:1 일치만 자동 연결하고, 동명이인·미일치는 미지정으로 보존하는 009 SQL과 동의자 이름 로그를 정적 검사한다. 실제 실행 및 계정별 동의 검증은 남아 있다.
- 2026-09-21 실제 009 SQL 실행 성공. 기존 3건 중 이름이 활성 회원과 일치한 2건이 `인수 동의 대기`로 전환됐고, 이름 미일치 1건은 자동 연결하지 않았다. 실제 지정 계정 동의 완료 및 비지정 계정 차단은 미검증이다.
- 관리자 수정·삭제 UI와 `010_handover_admin_manage.sql`의 관리자 전용 표·Storage 삭제 RLS를 정적 검사했다. 2026-09-21 실제 SQL 실행 성공으로 정책을 적용했다. 실제 관리자 수정·삭제, 일반회원 버튼 숨김·DB 차단, 사진 삭제 성공/실패 처리는 별도 실서버 검증이 필요하다.
- `knowledge-center.cjs`로 일반회원 메뉴 표시(Q&A·운행가이드), 관리자 UI·일반회원 열람, PDF 20MB 제한, `011_knowledge_center.sql`의 표·비공개 Storage·RLS 연결을 정적 검사했다. 2026-09-21 실제 SQL 실행 성공으로 표·비공개 Storage·권한 정책을 적용했다. 관리자 등록·수정·삭제, 일반회원 검색·열람·다운로드의 실제 브라우저 검증은 후속 실서버 검증이 필요하다.
- 메뉴 이동 시 해시 기록 생성과 `popstate` 기반 뒤로가기·앞으로가기 화면 복원은 `navigation-history.cjs` 정적 검사로 확인한다. 실제 브라우저의 여러 메뉴 이동 후 뒤로가기·앞으로가기 동작은 배포 후 확인한다.
- 차량·계약 표의 필터형 제목 내부 여백을 0으로 고정해, 제목과 아래 데이터 시작선이 맞도록 `table-column-alignment.cjs` 정적 검사를 추가했다. 실제 넓은 화면에서의 시각 정렬은 배포 후 확인한다.
- 차량현황·계약정보 업로드는 각각 상단 버튼 한 곳에서만 열리고, 팝업에서는 전체 너비 점선 선택 영역만 보이도록 `contract-upload-ui.cjs` 정적 검사를 추가했다. 실제 팝업 시각 확인은 배포 후 확인한다.
- 차량·계약 서버 조회 실패 시 빈 목록으로 임시 대체하지 않고 상태를 안내하며, 로그인 직후 한 번 재시도하고 비로그인 차량 임시 저장을 차단하는 `supabase-load-status.cjs` 정적 검사를 추가했다. 실제 배포 주소에서 로그인·새로고침 후 서버 목록 복원은 추가 확인이 필요하다.
- 차량계약정보 업로드 양식을 차량번호·렌탈료·계약시작·계약종료 네 열로 축소했다. `contract-upload-ui.cjs`와 `contract-storage.cjs`로 네 열 안내·양식 생성, 등록 차량번호 검증, 차량 ID 기반 계약 저장을 확인한다. 실제 Excel 업로드 후 차량현황의 조직·담당자·차종 표시 확인은 배포 후 진행한다.
- 비용마감 업로드 양식을 차량번호·렌탈료·주유비·통행료·주차비 다섯 열로 축소했다. `closing-ui.cjs`로 양식 열 생성과 미등록 차량번호 차단, 차량현황 조직 자동 연결 코드를 정적 검사한다. 실제 Excel 업로드 후 연결된 본부·부·팀과 확정본 유지 확인은 배포 후 진행한다.
- 배포 콘솔 오류 분석 결과, 제거된 권한 버튼을 숨기려다 로그인 흐름이 중단된 것을 확인했다. 버튼 존재 여부를 확인한 뒤 권한 UI를 적용하도록 보완했고 같은 정적 검사에 포함했다.
- 2026-09-22 Vercel 배포본에서 관리자 로그인 상태로 새로고침한 뒤 차량 10대, 계약 월합계 4,200,000원, 2026-08 확정 운행자료 표시를 실제 확인했다.
- 대시보드 부서별 운행 표를 차량번호별 이용일수·월 주행거리 상·하위 5대 카드로 교체했다. `dashboard-layout.cjs`와 `monthly-rules.cjs`로 화면 필수 영역과 고유 날짜·거리 합산 계산 회귀를 확인했다. 실제 배포 화면의 순위 배치는 배포 후 확인한다.
- 월별 렌트비용 추이를 제거하고, 확정 비용마감자료의 주유비·통행료·주차비를 항목별 선으로 표시하는 최근 12개월 그래프로 교체했다. 기준월 합계·미마감 표기와 기존 부대비용 요약의 연결을 `app.js` 문법 검사 및 대시보드 구조 검사로 확인한다. 실제 여러 확정월의 선·범례·작은 화면 시각 확인은 배포 후 진행한다.
- 오른쪽 차량 부대비용 요약 카드를 제거하고, 그래프 영역을 전체 너비로 확장했다. 표시 기간은 3~12개월 슬라이더로 변경했으며, 슬라이더 이동에 따른 월 수·축 간격·그래프 제목 변경은 문법과 대시보드 구조 검사로 확인한다. 실제 확정 자료가 있는 그래프에서 모든 슬라이더 값의 시각 검증은 배포 후 진행한다.
- 차량별 월 합계의 차량번호 버튼과 일별 상세 창을 추가했다. `monthly-rules.cjs`로 같은 차량·날짜의 거리 합산, 요일 표시, 월 이용일수·월 이동거리 일치를 모의 검사하고 실제 배포 화면에서 차량번호 선택과 상세 창 닫기를 추가 확인한다.
- 일별 상세의 주말·법정/대체공휴일 빨간색 구분과 Excel 내려받기를 추가했다. `monthly-rules.cjs`로 주말·공휴일 판정과 내려받기 열 구성을 정적·모의 검사하고, 실제 배포 화면에서 공휴일 색상 및 Excel 파일 열람을 추가 확인한다.
- 사고접수 메뉴·차량 기본정보 연결·사진/PDF 최대 3개 제한·대차 차량번호·관리자 처리 코멘트·상세 접기 UI와 `014_vehicle_accidents.sql`의 활성 회원/관리자 RLS를 정적 검사했다. 2026-09-23 Supabase SQL Editor에서 014·015·016 실행 성공으로 표·버킷·권한 정책·RPC·PDF 허용 형식·대차 차량번호 열을 적용했다. 차량번호 입력 시 후보 표시·선택과 차량 기본정보/사고일 위치 교체를 정적 검사한다. 실제 접수·첨부 업로드·일반회원 코멘트 차단·관리자 코멘트 저장은 별도 실서버 검증이 필요하다.
- 사고접수 메뉴가 기존 다음 단계 안내로 떨어지던 오류를 수정했다. `accident-ui.cjs`에 메뉴 이동 허용 조건 검사를 추가했다. 실제 배포 화면에서 메뉴 클릭 후 접수 화면 표시를 확인한다.
- 사고접수·인수인계 이력은 작성자 본인 또는 관리자의 수정·삭제 버튼만 표시하고, `017_author_manage_accidents_handovers.sql`에서 같은 조건을 RLS로 강제한다. 2026-09-23 Supabase SQL Editor 실행 성공으로 정책을 적용했다. 실제 작성자·다른 일반회원·관리자 계정별 허용/차단과 첨부 삭제는 배포 후 확인한다.
- 인수인계·사고 이력의 상세는 선택한 행 바로 다음 `tr`에 삽입하고, 연한 회색 상세 스타일과 접기 제거 동작을 정적 검사한다. 실제 여러 이력에서 선택·접기·스크롤 위치는 배포 후 확인한다.
- 인수인계·사고 이력 상세의 사진·PDF 첨부 카드가 78px 높이·작은 격자로 표시되는 스타일을 정적 검사한다. 실제 다중 PDF의 줄바꿈·파일 열기는 배포 후 확인한다.
- 종 알림 팝오버와 사이드바 접기·펼치기·고정 설정은 `sidebar-controls.cjs` 정적 검사로 화면 ID·로컬 저장·스타일 연결을 확인한다. 실제 넓은 화면에서 고정/해제, 새로고침 후 복원, 작은 화면 팝오버 위치는 배포 후 확인한다.

## 2026-09-17 업무 기준 적용 결과

- node --check 두 JS 파일 및 node tests/monthly-rules.cjs 통과.
- 브라우저 직접 입력 8월 1200km → 확정 → 두 월 CSV 업로드: 8월 유지, 9월 800km 표시.
- 새 v2 익명 보관파일 복원: 7월 700km와 당시 조직정보 표시, 기존 월 유지.
- 비용 CSV 업로드·9월 확정: 렌트비 115만원, 부대비용 165000원, 합계 1315000원 연동.
- 평균선 모든 점의 y좌표 동일 확인. 인쇄 보고서 생성내용과 콘솔 오류 없음 확인.
- 파일을 실제 내려받아 재접속 복원하는 전체 흐름, 실제 인쇄/PDF, xlsx 원본 업로드, 관리자 정정 회귀검증은 이번에 확인하지 않음.

이 문서는 점검 기준이다. 체크 항목이 있다는 이유로 검증 완료로 간주하지 않는다.
2026-09-17 리팩터링의 실제 결과와 미확인 범위는 code-structure.md에 보관되어 있다.

## 공통

- [ ] 관련 요구사항과 완료 기준 확인
- [ ] node --check dist/js/app.js
- [ ] node --check dist/js/dialogs.js
- [ ] 실제 브라우저 화면·팝업·콘솔 오류 확인
- [ ] 배포했다면 완료 상태와 배포된 CSS·JS 로딩 확인

## 입력과 내려받기

- [ ] 양식 첫 행 제목, 파일 내용과 날짜·숫자 형식 확인
- [ ] 해당 양식으로 업로드 후 목록 표시 확인
- [ ] 직접 추가·수정, 미매칭·빈 값·중복·잘못된 날짜 확인
- [ ] 자료 내려받은 파일 내용과 화면 합계 일치 확인

## 월별 마감과 보고

- [ ] 서로 다른 두 월 자료를 올려 기존 월 유지 확인
- [ ] 대상 월 과거 조회와 확정 상태 확인
- [ ] 정정 전 확정본 유지, 사유 기록, 재확정·버전 조회 확인
- [ ] 재접속 후 JSON 복원 또는 서버 저장 복원 확인
- [ ] 현재 조직 변경 후 과거 보고 유지 확인
- [ ] 계약합계, 그래프 평균, 월경계 거리, 미집계와 0원 구분 확인
- [ ] 실제 권한 연결 후 허용·차단 모두 확인

## 작업별 결과 기록

### 2026-09-18 — 운행 SQL 적용·배포 코드 확인

- 사용자 확인: 003_driving_months.sql 실행 성공.
- 배포 driving-months.js HTTP 200 및 save_driving_month 호출 코드 포함 확인.
- 로그인 후 실제 업로드·확정·새로고침 복원·서버 권한은 아직 미검증이다.

### 2026-09-18 — 운행 월 집계·페이지 이동

- monthly-rules: 같은 차량 20/30/40km → 90km·2일, 확정 조직 유지, 21행 페이지 이동, 재업로드 교체, 확정월/복수월 차단, 저장 실패 시 원자료 유지 검증 (mock).
- JavaScript 문법·closing-ui 회귀검증 통과.
- 실제 SQL·권한·업로드·두 월 확정·재접속·브라우저 화면은 미검증이다.

### 2026-09-18 — 계약기간 자동 계산

- 계약 화면의 총개월·잔여개월 계산과 종료 상태 표시를 확인했다.
- 월 경계·역순 기간·종료 계약 테스트는 실제 브라우저 검증에서 날짜와 함께 확인한다.

### 2026-09-17 — 차량인수인계

- app.js/handover.js 문법 및 기존 closing-ui/monthly-rules 회귀검증 통과.
- handover: 사진 개수·크기·허용 데이터 형식 검증 (mock). 이미지 실제 디코딩은 포함하지 않는다.
- 실제 사진 선택·미리보기·저장·JSON 복원 브라우저 전 과정은 미검증. 서버 저장·권한은 미연결.

### 2026-09-17 — 운행 보관함 헤더 간소화

- 기존 상태·확정 버튼 ID 유지. closing-ui의 HTML ID 중복 검사와 monthly-rules 회귀검증 통과.
- 실제 브라우저 레이아웃과 배포는 이번 검증에 포함하지 않았다.

### 2026-09-17 — 마지막 운행 확정 기준

- 자동 검증: 최근 확정 시각과 최대 월도 구분, 임시자료 제외, 확정본 없음 표시.
- 실제 브라우저 레이아웃·파일 업로드/복원은 미검증. 운행 서버 저장은 미연결.

### 2026-09-17 — 비용 양식 제목 순서

- closing-ui에서 양식 생성 함수에 전달되는 8개 제목의 순서를 화면 기준으로 검증했다 (mock).
- JavaScript 문법 및 기존 비용표·확정월 추출 회귀검증 통과. 실제 브라우저 다운로드 파일 열기는 미검증.

### 2026-09-17 — 비용표 합계·확정월 추출

- closing-ui: 차량별/전체 합계, 두 월 확정본의 JSON 직렬화·복원 후 과거 월 추출, 조직값 유지, 미확정월 추출 차단 검증 (mock DOM).
- 실제 Excel 다운로드 파일 열기, 업로드·정정·복원 전체 브라우저 흐름, 배포는 미검증.

### 2026-09-17 — 비용마감 UI 정리

- JavaScript 문법 검사와 monthly-rules, contract-storage, vehicle-delete-all 회귀검증 통과.
- closing-ui 검증: HTML ID 중복·누락, 빈 자료 표시, 검토자료 합계, 확정 잠금, 정정 중 재확정 버튼 상태 확인 (mock DOM).
- 실제 브라우저 시각 검증, 실제 Excel 업로드·두 월 마감·JSON 복원 및 배포 확인은 이번 검증에 포함하지 않았다.
- 진행 상태와 데이터 구조의 오래된 연결 설명을 현재 구현 기준으로 갱신했다.

### 2026-09-18 — 대시보드 재배치

- 후속 변경: 상단 3개 지표 순서·만료 지표 위치·부서 목록 높이 제한/스크롤 제거를 dashboard-layout에서 검증. closing-ui 통과. 실제 브라우저 시각 확인·배포는 미검증.

- dashboard-layout으로 HTML의 전체 지표·부서현황·만료요약·운행현황·비용 순서 및 보고월/기준월 유지 검증. closing-ui/monthly-rules 회귀검증 통과.
- 실제 브라우저 시각 확인·배포는 미검증.

### 2026-09-18 — 대시보드 운행·계약 요약

- 문법 검사와 monthly-rules/closing-ui 통과. 확정자료와 검토자료를 다르게 구성해 확정 원본만 거리·차량·이용일수 집계하는 mock 검증 추가.
- 계약 목록 제거와 6개월 조건은 코드 확인. 실제 브라우저·서버·배포 및 날짜 경계 동작은 미검증.

### 2026-09-18 — 운행 상단 조직 요약 검증

- JavaScript 문법 검사, monthly-rules와 closing-ui 통과.
- 익명 mock: 반복 차량번호 정규화, 5대/1본부/2부/4팀, 서로 다른 부의 동명 팀 구분, 전체거리 70km, 확정자료 없음 확인.
- 실제 브라우저 레이아웃·배포는 이번 검증에 포함하지 않음.

### 2026-09-18 — 운행 SQL 실제 재실행

- 사용자 승인 후 실제 프로젝트 SQL Editor에서 수정된 003 SQL 실행 성공.
- 표·함수 존재 true/true, 인수 p_month text/p_rows jsonb/p_confirm boolean DEFAULT false, authenticated 실행 권한 true 확인.
- 업무 원본자료는 변경하지 않았다. 실제 파일 업로드·확정·두 월 보관·재접속 및 역할별 허용/차단은 미검증.

날짜 / 작업·커밋 / 환경 / 익명 샘플 / 실행 항목 / 결과 / 미검증 범위 / 후속 작업을 기록한다.
문법 통과, 화면 표시, 다운로드 안내만으로 전체 기능 또는 저장파일 검증을 대신하지 않는다.

### 2026-09-26 — 운영 배포 복구

- GitHub `main`이 `69a52f2`임을 확인했고, Vercel에서 해당 커밋을 운영 배포로 직접 생성했다.
- Vercel 배포 상태 `Ready` 확인. 운영 주소의 새 요청에서 사고접수 메뉴, CC 포함 양식 안내, 조직 수 배지의 `14.4px`/`8px 11px` 스타일을 확인했다.
- 로그인하지 않은 점검 탭에서는 보호된 차량·계약·운행 자료가 0건으로 표시되는 것을 확인했다. 실제 계정 로그인 후 자료 표시 재검증은 남아 있다.
