# 법인차량 관리 홈페이지 개발 가이드 — 검토안

작성일: 2026-09-17
상태: 사용자 검토용. 이 문서를 생성하는 과정에서 홈페이지 코드나 배포 설정은 변경하지 않았다.

후속 결정: 사용자가 6가지 기준을 확정했다. 최신 기준은 docs/data-rules.md를 따른다. 아래 누적 계기판·적용기간 배분·유효 계약·평균선 선택에 관한 제안은 최신 확정사항보다 우선하지 않는다. 코드 적용은 별도 작업이다.

## 1. 권장 방향

기존 디자인과 요구사항을 유지하고, 현재 프로젝트 안에서 데이터 구조와 코드를 단계적으로 정리한다.
전체를 처음부터 다시 만들면 이미 확인한 화면과 요구사항을 재검토하는 비용이 든다.
반면 현재 한 HTML 파일에 계속 기능만 추가하면 수정 영향 범위가 커지고 업로드·조회·계산 오류가 반복되기 쉽다.
권장안은 '기존 프로젝트를 이어서 개발하되, 실사용 전 기반을 정리하는 것'이다.
프레임워크 교체는 필수가 아니며, 코드 분리 이후 필요성을 판단한다.

## 2. 현재 상태와 미완료 사항

- 저장소: GitHub gyeongtaek4/krryugt.
- 배포: Vercel https://krryugt-gray.vercel.app/.
- 최초 상태는 화면·스타일·데이터·계산·업로드가 한 HTML에 들어 있었다. 이후 리팩터링으로 `dist/index.html`(306줄), `dist/css/app.css`, `dist/js/dialogs.js`, `dist/js/app.js`로 분리했다. 역할과 검증은 `docs/code-structure.md`를 참고한다.
- 차량현황: 조직, 정/부 담당자, 차량번호, 차종, 지역, 주차장. 직접 추가·수정, Excel 업로드·자료 내려받기·양식 내려받기 구현.
- 차량계약정보: 조직, 담당자, 차량, 월 렌탈료, 계약시작/종료 및 기간 표시. 입력·업로드·내려받기 구현.
- 운행기록: 차량번호, 키로수, 운행일자 업로드 및 월별 조회, 차량현황 연결, 확정자료 JSON 보관·복원 구현.
- 운행 화면의 부서별 분석 표와 운행일수 요약은 제거했다. 차량별 기록 목록은 유지한다.
- 비용마감: 월도 선택, 자료 업로드, 확정, JSON 보관·복원, 관리자 정정 모드 구현.
- 비용 정정 시 이전 확정본과 정정 사유가 JSON 이력에 남지만, 상세 이력 조회 화면은 아직 없다.
- 관리자 마감 수정은 비용마감에만 구현되어 있다. 운행 마감 정정 기능은 미구현이다.
- Supabase, 실제 로그인, 실제 관리자 권한, 서버 영구 저장은 미연결이다.
- 화면의 '관리자' 표시나 버튼 숨김은 실제 권한 통제가 아니다.
- 새로고침 후 JSON 보관파일을 불러와야 확정자료를 복원한다. 현재 차량/계약 입력자료의 서버 영구 저장도 없다.
- 전체 운영차량, 부서별 차량 현황, 계약만료 대상 등 대시보드 일부는 예시값이다.
- 계약금액 카드와 비용 추이는 데이터에서 계산하지만 모든 보고 지표가 실제 데이터와 연결된 것은 아니다.
- 현재 주행거리 계산은 같은 월 최대/최소 누적 키로수 차이로, 첫 기록 이전의 주행거리를 빠뜨릴 수 있다.
- 그래프의 큰 금액은 조회기간 평균이고, 선은 여전히 3개월 이동평균이다. 최종 선의 의미를 확정해야 한다.
- 직전 화면 수정에서 조회월 입력칸이 제거되어 업로드 오류가 발생했고 복원했다. 화면 요소와 기능의 연결을 검증하는 절차가 필요하다.

현재 구현 여부와 실제 데이터로 검증한 여부를 별도로 관리한다. 화면에 버튼이 있는 것만으로 실사용 완료로 판단하지 않는다. STEP 2 중 파일 분리는 완료했으나 기능별 JavaScript 모듈화와 자동 회귀검증 체계는 후속 작업이다.

## 3. 다섯 가지 핵심 목적과 화면

| 목적 | 관리할 자료 | 최종 결과 |
|---|---|---|
| 비용 마감 | 월별 차량별 렌트비·주유비·통행료·주차비, 확정 버전 | 과거 월 조회, 전월/전년 동월 증감, 총 비용 및 부서별 비용 |
| 운행데이터 | 차량별 일자·실제 주행거리 또는 누적계기판 값, 배정 이력 | 차량별 이용일수·월 주행거리, 부서별 배치 검토 |
| 차량계약정보 | 계약별 기간·월 렌탈료·차량·업체 | 현재 계약금액, 만료 예정, 갱신/반납 검토 |
| 차량담당자 | 차량, 정/부 담당자, 조직, 지역/주차장, 적용기간 | 현재 배정표와 과거 배정 이력 |
| 경영진 보고 | 확정된 비용/운행 집계 및 현재 계약/배정 | 기준월이 명확한 대시보드와 보고자료 |

추천 메뉴: 대시보드 / 차량·담당자 현황 / 차량계약정보 / 운행기록데이터 / 월별 비용마감자료.
차량담당자를 별도 메뉴로 분리할지는 이후 데이터 규모와 사용 흐름에 따라 판단한다.
운행 입력 화면은 차량별 상세 중심으로 유지하고 부서별 비교는 보고 화면에서 제공한다.

## 4. 개발 전 반드시 확정할 계산과 보관 규칙

### 현재 정보와 과거 자료

- 현재 차량정보와 월별 확정자료를 분리한다. 현재 담당자 변경으로 과거 보고 수치가 바뀌어서는 안 된다.
- 각 차량에는 내부 고유 ID를 사용한다. 차량번호는 업로드 매칭에 사용하되 공백 등을 정규화하고 번호 변경도 처리한다.
- 배정 이력에는 적용시작일/종료일을 둔다. 업로드 시점의 현재 부서만 사용하면 늦게 올린 과거 자료의 부서가 잘못 연결될 수 있다.
- 마감 때 비용·운행자료와 해당 기간의 조직/배정 기준을 고정한다. 월중 부서 이동을 어느 부서에 배분할지 규칙을 정한다.
- 업로드일과 마감월도를 구분한다. 8월 자료를 9월에 올려도 8월 자료로 보관한다.
- 원본 Excel, 업로드 내역, 확정 버전, 정정 버전을 함께 추적한다.

### 비용과 증감

- 실제 비용: 해당 마감월의 확정 렌트비 + 주유비 + 통행료 + 주차비.
- 계약금액: 조회 기준일에 유효한 계약의 월 렌탈료 합계. 실제 청구비용과 구분한다.
- 부가세 포함/별도, 비용 귀속월, 계약 중도변경·환급 처리 기준을 통일한다.
- 증감액 = 해당월 확정금액 - 비교월 확정금액.
- 증감률 = 증감액 / 비교월 확정금액 × 100. 비교월이 미마감이거나 0원이면 '비교 불가'로 표시한다.
- 미집계와 0원은 다르다. 미마감 월을 0원으로 그래프나 평균에 포함하지 않는다.
- 조회기간 평균 = 그래프에 표시된 확정월 합계 / 확정월 수.
- 평균선은 조회기간 고정 평균선과 3개월 이동평균 중 선택한다. 명칭과 계산이 일치해야 한다.

### 주행거리와 이용일수

- 렌터카 제공 '키로수'가 일별 실제 거리인지 누적 계기판 값인지 먼저 확인한다.
- 일별 실제 거리라면 해당월 거리를 합산한다.
- 누적 계기판 값이라면 월 시작/종료 기준값과 전월 마지막 기록을 이용하는 규칙을 정한다.
- 한 달에 기록 하나만 있는 차량을 자동으로 0km로 판단하지 않는다. 기준값 부족 상태를 표시한다.
- 계기판 값 감소, 계기판 교체, 날짜 누락을 검토 대상으로 분류한다.
- 차량 이용일수는 단순 업로드 행 수가 아니라 실제 운행이 확인된 서로 다른 날짜 수를 기준으로 한다.
- 매일 기록되지만 계기판 값이 같은 자료를 운행일로 셀지 검토한다. 월 총거리만 있으면 일수는 계산할 수 없다.
- 부서 배치 검토에는 차량별 월 거리, 이용일수, 가용일수 대비 이용률, 차량당 비용을 사용한다.
- 부서 이용률은 차량별 이용일수 합계 / 차량별 가용일수 합계로 정의할 수 있다. 휴일·입출고일 포함 기준은 확정이 필요하다.
- 낮은 이용률을 발견했다고 자동으로 차량을 이동시키지 않는다. 대기·장거리 출장 등 업무 특성을 같이 검토한다.

### 확정과 정정

- 임시자료 → 검증 → 마감 확정 → 관리자 정정본 작성 → 재확정 순서로 운영한다.
- 정정 중에는 직전 확정본을 보고에 사용한다. 재확정이 완료되면 새 버전을 보고 기준으로 적용한다.
- 이전 확정본을 덮어쓰거나 삭제하지 않는다. 버전, 정정 사유, 처리자, 처리일을 저장한다.
- 비용과 운행은 별도 마감 상태를 관리한다. 선택월에서 어느 자료가 확정됐는지 보여준다.
- 과거 보고서는 마감 버전을 표시해 같은 수치를 다시 재현할 수 있게 한다.
- 관리자 권한과 마감 상태 검사는 데이터베이스에서도 강제한다.

## 5. 추천 데이터 구조

아래는 초안이며 실제 Excel 샘플과 업무 규칙을 확인한 뒤 확정한다.

| 데이터 묶음 | 역할 |
|---|---|
| 조직/담당자 | 본부·부·팀과 담당자 정보 |
| 차량 | 고유 ID, 차량번호, 차종, 지역/주차장 |
| 차량 배정 이력 | 차량의 부서·정/부 담당자와 적용기간 |
| 계약 | 차량과 계약별 시작/종료, 월 렌탈료, 업체 |
| 업로드 내역 | 원본 파일, 자료 유형, 업로드일, 처리결과 |
| 운행기록 | 차량별 운행일, 누적 키로수 또는 실제 거리 |
| 마감 버전 | 비용/운행 유형, 귀속월, 버전, 확정자/일시, 상태 |
| 마감 상세 | 해당 버전의 차량별 비용/운행 및 당시 조직정보 |
| 정정 이력 | 이전/새 버전 연결, 사유와 처리자 |

원본 파일은 Supabase Storage의 비공개 영역, 구조화된 자료는 데이터베이스에 보관하는 방향을 검토한다.
사이트 주소를 아는 사람에게 자료가 열리지 않도록 로그인과 데이터 접근 권한을 구현한다.
코드는 GitHub, 업무 데이터는 데이터베이스/파일 저장소에 분리한다. 비밀번호·비밀키·실제 담당자 명단은 MD나 Git 저장소에 넣지 않는다.
서버 영구 저장, 권한, 복구 방법이 확인되기 전까지 실제 업무의 유일한 자료 저장소로 사용하지 않는다.

## 6. STEP별 개발 계획과 완료 기준

| STEP | 수행할 일 | 완료 기준 |
|---|---|---|
| 1. 요구사항 확정 | 실제 형식의 익명 Excel 샘플 확인, 월도·세금·거리·일수·정정 규칙 결정 | 항목과 계산 예시를 사람이 확인 |
| 2. 개발 기반 정리 | 현 버전 보존, 화면/스타일/데이터/계산/업로드 코드 분리, 검증·배포 절차 마련 | 기존 주요 흐름이 동작하고 테스트 배포에서 확인 |
| 3. 영구 저장과 권한 | Supabase 데이터 구조, 로그인, 관리자/입력자/조회자 권한, 파일 보관·복구 구현 | 재접속/다른 PC에서 자료 조회, 권한 없는 변경 차단, 복구 확인 |
| 4. 차량·담당자·계약 | 입력·수정·업로드·내려받기 통일, 배정 이력, 계약 기간·유효 계약 계산 | 번호 매칭과 부서 변경 이력, 계약금액·만료 목록 정확성 확인 |
| 5. 월별 비용 마감 | 미리보기·검증·확정·정정 버전·과거 조회·증감 비교 | 8월/9월 자료 동시 보관, 8월 정정 후 원본/정정본 재조회 |
| 6. 차량별 운행 분석 | 거리 계산, 차량별 이용일수, 월별 확정과 정정, 보고용 부서 집계 | 기준값 누락·중복·계기판 감소 처리와 월경계 거리 검증 |
| 7. 경영진 보고 | 실제값 연동, 기준월·자료상태·버전 표시, 12개월 그래프, 보고자료 출력 | 원본·마감·대시보드·내려받은 보고 수치가 일치 |
| 8. 시범 운영 | 2~3개월 자료, 부서 이동·정정·접속 복원 등으로 검증 | 사용자 확인 후 실제 운영, 운영 안내와 백업 담당자 지정 |

우선순위는 비용마감이 가장 높지만, 정확한 마감을 위해 차량·조직·계약의 기본 구조를 먼저 정리한다.
전체 개발기간이나 비용은 실제 자료량과 계정/권한 요구를 확인한 뒤 산정한다.

## 7. MD 파일의 역할과 최소 구성

MD는 업무 규칙과 개발 상태를 기록하는 텍스트 문서다. 파일 자체가 기능을 구현하거나 데이터를 저장하지는 않는다.
이 가이드는 검토안이며, 방향 결정 후 아래 파일들에 확정 내용을 나눈다.

| 파일 | 기록할 내용 | 관리 원칙 |
|---|---|---|
| README.md | 프로젝트 목적, 시작 방법, 주요 명령, 배포와 관련 문서 위치 | 처음 보는 사람이 시작할 수 있게 작성 |
| AGENTS.md | 개발자가 지켜야 할 핵심 규칙, 관련 문서 경로, 실제 검증 명령 | 짧게 유지. 프로젝트의 모든 설명을 넣지 않음 |
| docs/requirements.md | 확정 기능, 화면별 항목, 계산/마감 규칙, 제외 범위 | 사업 요구사항의 기준 문서 |
| docs/data-model.md | 데이터 관계, 매칭/중복 기준, 배정 이력, 마감 버전/권한 | 저장 구조를 변경할 때 갱신 |
| docs/roadmap.md | 현재 STEP, 완료/미완료, 결정 필요 항목, 다음 작업과 완료 기준 | 최근 상태를 간결하게 갱신 |

AGENTS.md는 Codex가 프로젝트 지침으로 읽는 특별한 이름이다. 다른 MD는 이름만으로 모두 자동 적용되는 것이 아니므로 관련 작업에서 경로를 지정한다.
이 문서 작성 이후 AGENTS.md를 생성했고, 2026-09-17 문서 관리 요청에 따라 주제별 문서를 추가했다. 최신 문서 목록은 AGENTS.md를 참고한다. 이 가이드의 제안사항 전체가 승인된 것은 아니다.

AGENTS.md에 넣을 핵심 규칙 초안:

```md
# 개발 지침
- 업무 규칙: docs/requirements.md. 저장 구조: docs/data-model.md.
- 현재 작업과 완료 기준: docs/roadmap.md.
- 흰 배경, #01916d / #99d3c5 / #e6f4f0, 보조 회색 #adadad / #d6d6d6 유지.
- 현재 차량정보 변경으로 확정월 자료와 이전 보고 버전을 바꾸지 않는다.
- 정정본은 이전 버전을 보관하고 관리자 권한을 데이터 저장 단계에서 확인한다.
- 미집계와 0원을 구분하고, 기준값 부족 주행거리는 임의로 0으로 처리하지 않는다.
- 업로드 변경 시 월 선택·중복·미매칭·과거 보관 흐름을 확인한다.
- 완료 후 검증 결과와 다음 작업을 roadmap에 갱신한다.
```

설치하지 않은 도구의 명령을 검증 명령처럼 기록하지 않는다. STEP 2에서 실제 사용할 명령을 추가한다.

## 8. 토큰과 재작업을 줄이는 작업 방식

- 긴 대화를 매번 복사하기보다 확정 문서와 해당 STEP을 지정한다.
- AGENTS.md는 반복 규칙만, 업무 규칙은 requirements, 최신 상태는 roadmap에 기록한다. 같은 설명을 여러 문서에 복제하지 않는다.
- 한 작업은 하나의 기능 흐름과 완료 기준을 포함한다. 작은 오류 하나씩 끝없이 대화하거나 한 번에 전체를 만들게 요청하지 않는다.
- 목표·관련 문서·변경 범위·완료 기준을 짧게 전달한다. 화면 설명만 있을 때는 실제 파일과 재현 순서를 함께 지정한다.
- 오류를 공유할 때 화면, 오류문구, 파일 유형, 어떤 순서로 발생했는지를 한 번에 전달한다. 익명 자료를 사용한다.
- 화면 문구/색상 변경과 저장 구조/계산/권한 변경을 구분해 검증 깊이를 조절한다.
- 단순 문구·스타일 수정은 가벼운 작업 설정, 계산·마감·권한 설계는 충분한 검토를 사용한다. 구체적 모델과 가격은 실행 시 확인한다.
- 관련 코드만 읽도록 파일을 분리하고 공통 업로드·내려받기 처리를 재사용한다.
- 각 작업은 구현 → 핵심 검증 → 문서 상태 갱신 → 테스트 배포 확인 → 운영 반영으로 끝낸다.
- 문서가 길면 입력 토큰도 늘어난다. MD 생성 자체가 비용 절감을 보장하지 않는다.
- 정확한 토큰비용은 사용 제품/플랜/모델에 따라 달라지므로 이 문서에서 금액이나 절감률을 단정하지 않는다.
- 이 홈페이지의 표·합계·그래프는 일반 코드로 계산하면 된다. 기본 기능에 AI API를 넣을 필요는 없다.

추천 작업 요청 예시:

```text
docs/requirements.md와 docs/data-model.md를 기준으로
roadmap의 STEP 5 중 비용 마감 과거조회와 정정 버전을 구현해줘.
완료 기준: 8월·9월 자료가 함께 남고, 8월을 정정해도 이전 확정본을 조회할 수 있을 것.
검증 후 roadmap에 완료 상태와 남은 문제를 기록해줘.
```

## 9. 사용자가 결정할 항목

1. 기존 디자인을 유지하고 단계적으로 기반을 정리하는 권장안을 선택할지.
2. 실제 키로수가 누적값인지, 실제 거리인지 및 자료에 운행 여부가 포함되는지.
3. 비용의 세금/귀속월 기준과 월중 부서 이동의 배분 기준.
4. 일반 입력자·관리자·경영진의 실제 조회/변경 범위.
5. 경영진 보고에 필요한 지표와 Excel/PDF 등 출력 형식.
6. 렌트비용 그래프 평균선의 최종 계산 의미.

방향이 결정되면 STEP 1의 요구사항 문서를 먼저 확정하고 다음 STEP으로 진행한다.

## 10. 공식 참고 문서

- [OpenAI — AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md)
- [OpenAI — Codex 사용 모범 사례](https://learn.chatgpt.com/guides/best-practices)
- [Supabase — 데이터 접근 권한/RLS](https://supabase.com/docs/guides/database/postgres/row-level-security)
- [Supabase — 원본 파일 Storage](https://supabase.com/docs/guides/storage)
