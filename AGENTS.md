# 법인차량 관리 홈페이지 개발 원칙

## 목적과 우선순위

- 회사가 장기간 사용할 법인차량 관리 홈페이지를 개발한다.
- 핵심 목표: 비용 마감과 월별 자료 축적·증감 비교, 운행자료를 활용한 차량 배치 검토, 차량계약 관리, 담당자·배정 관리, 경영진 보고.
- 사용자는 초보자다. 결과와 사용 방법을 쉬운 한국어로 설명한다.
- 기존 디자인과 확인된 기능을 활용하고 단계적으로 개선한다. 구조 변경은 목적과 영향 범위를 먼저 설명한다.

## 참고 문서와 현재 구조

- `DEVELOPMENT_GUIDE.md`: 개발 방향, 현재 상태, STEP별 계획을 정리한 검토안. 미결정 항목을 확정 요구사항으로 취급하지 않는다.
- `README.md`: 프로젝트 안내 및 사용 방법.
- 현재 구조: `dist/index.html`은 화면 골격, `dist/css/app.css`는 스타일, `dist/js/dialogs.js`는 팝업 구성, `dist/js/app.js`는 데이터·기능 코드다.
- `docs/code-structure.md`: 파일 역할, 실행 순서, 미리보기 방법과 리팩터링 검증 결과. 구조를 변경할 때 갱신한다.
- GitHub 저장소는 `gyeongtaek4/krryugt`, 배포 서비스는 Vercel이다.
- 추후 상세 업무 규칙은 `docs/requirements.md`, 저장 구조는 `docs/data-model.md`, 진행 상태는 `docs/roadmap.md`에 관리한다. 파일이 실제로 존재할 때 참고한다.

## 문서 생성과 갱신

- 계속 참고해야 할 요구사항, 계산 기준, 데이터 구분, 설계 결정, 운영 방법, 진행 상태가 생기면 같은 작업에서 MD 문서로 기록한다. 별도 문서 작성 요청을 기다리지 않는다.
- 같은 주제의 문서가 있으면 먼저 갱신한다. 새로운 주제는 역할이 명확한 이름으로 `docs/`에 생성한다. 단발성 대화마다 파일을 만들지 않는다.
- 확정사항, 제안사항, 미결정사항, 구현 여부, 실제 검증 여부를 구분한다. 사용자 승인 전 제안을 확정된 규칙으로 기록하지 않는다.
- 문서를 생성하거나 역할을 변경하면 이 파일의 참고 문서 목록도 갱신한다. 반복 적용할 기본원칙이 바뀌면 해당 원칙도 갱신한다.
- 상세 내용은 주제별 문서에 두고, 이 파일에는 핵심 원칙과 문서 위치를 기록한다. 같은 설명을 여러 파일에 중복하지 않는다.
- 작업 완료 시 생성·갱신한 문서와 중요한 변경 내용을 사용자에게 간결하게 안내한다.

## 데이터 보관과 마감

- 현재 차량·담당자·계약 정보와 과거 월별 확정자료를 분리한다.
- 새 월 자료 업로드로 다른 월 자료를 삭제하지 않는다. 업로드일과 자료 귀속월을 구분한다.
- 현재 담당자나 부서 변경으로 과거 확정자료와 보고 버전이 바뀌지 않게 한다.
- 과거 조직 매칭은 배정 적용기간을 고려한다. 현재 부서를 과거 부서로 임의 대입하지 않는다.
- 임시자료 검토 → 마감 확정 → 관리자 정정 → 재확정 흐름을 유지한다.
- 정정 시 이전 확정본, 정정 사유, 버전, 처리자·처리일을 보관한다. 재확정 전 보고 기준은 직전 확정본이다.
- 비용과 운행의 마감 상태를 별도로 관리하고, 월별 조회에서 자료 상태를 표시한다.
- 서버 저장이 연결되기 전에는 JSON 파일 보관·복원 방식의 한계를 정확히 안내한다. 영구 저장되지 않는 자료를 영구 보관된 것처럼 설명하지 않는다.

## 계산과 자료 연결

- 차량번호의 공백 등을 정규화하여 매칭한다. 미매칭·중복·누락 자료를 사용자가 확인할 수 있게 한다.
- 계약상 월 렌탈료 합계와 실제 확정 비용을 구분한다. 보고 금액의 기준월·집계 범위를 명확히 한다.
- 미집계와 0원을 구분한다. 비교 자료가 없거나 비교금액이 0원이면 임의의 증감률을 표시하지 않는다.
- 주행거리 계산 전 키로수가 실제 거리인지 누적 계기판 값인지 확인한다. 월 시작 기준값이 부족하면 임의로 0km로 확정하지 않는다.
- 이용일수는 단순 행 수나 부서 내 날짜 합집합으로 차량 이용률을 대신하지 않는다. 실제 운행 확인 기준과 가용일수 기준을 정한 뒤 계산한다.
- 세금, 월중 부서 이동, 거리·일수, 평균선 등 미결정 규칙은 문서에 표시하고, 보고 결과를 바꾸는 선택은 사용자와 확정한다.
- 보고 지표는 실제 데이터와 연결한다. 예시값과 실제값을 구분하고 계산은 일반 코드로 처리한다.

## 화면과 Excel

- 흰 배경과 `#01916d`, `#99d3c5`, `#e6f4f0`을 유지한다. 보조 회색은 `#adadad`, `#d6d6d6`을 사용하며 작은 글자의 가독성을 확인한다.
- 내려받기 명칭은 `양식 내려받기`와 `자료 내려받기`로 구분한다.
- 업로드 양식 첫 행에는 필요한 열 제목을 넣고, 해당 양식을 채운 파일을 같은 화면에서 업로드할 수 있게 한다.
- 운행 입력 화면은 차량별 기록 중심으로 유지한다. 부서별 배치 비교는 보고 목적에 맞춰 제공한다.
- 화면 요소를 제거·이동할 때 업로드, 조회월 선택, 직접 입력, 마감, 내려받기 기능의 연결을 함께 확인한다.

## 권한과 개발·검증

- 관리자 표시나 버튼 숨김을 실제 권한 통제로 취급하지 않는다. Supabase 연결 시 로그인과 데이터 저장 단계의 권한을 구현한다.
- 실제 담당자 명단, 업무 원본자료, 비밀번호, 비밀키를 코드·MD·Git 저장소에 넣지 않는다. 검증에는 익명 자료를 사용한다.
- 한 작업의 목표·변경 범위·완료 기준을 정하고 관련 파일부터 확인한다. 변경과 무관한 사용자 파일을 보존한다.
- 기능별 코드와 공통 업로드·내려받기 처리를 정리해 관련 부분만 수정할 수 있게 한다.
- HTML에 대량의 CSS·JavaScript를 다시 넣지 않는다. 팝업을 구성한 후 기능 코드를 실행하는 순서를 유지한다. 배포는 `dist/` 전체를 포함한다.
- 로컬 미리보기: `node scripts/preview.cjs`. JavaScript 문법 확인: `node --check dist/js/app.js`, `node --check dist/js/dialogs.js`. 문법 확인과 실제 기능 확인을 구분한다.
- 검증 도구는 실제 설치된 것만 사용한다. 존재하지 않는 테스트 명령을 실행 가능한 것처럼 안내하지 않는다.
- 업로드·저장 변경은 익명 샘플로 실제 입력부터 목록 표시까지 확인한다. 문법 검사만으로 업로드 성공을 단정하지 않는다.
- 마감 변경은 두 월의 보관, 과거 조회, 정정 후 원본 유지, 재접속 복원을 확인한다. 권한 변경은 허용·차단을 모두 확인한다.
- 배포한 경우 완료 상태를 확인하고, 실제로 확인하지 않은 동작은 검증 완료라고 말하지 않는다.
- 완료 시 변경 결과, 확인한 범위, 남은 문제를 간결하게 기록한다. 진행 문서가 있으면 최신 상태로 갱신한다.
- 이 파일에는 반복되는 핵심 원칙만 둔다. 상세 설명과 변경 이력은 관련 문서에 두고 중복하지 않는다.
